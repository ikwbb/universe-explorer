import type {Mission} from '../data/spacecraftCatalog';import {dateLabel} from '../utils/format';
export default function MissionTimeline({mission,time,onDate}:{mission:Mission;time:number;onDate:(time:number)=>void}){const events=[{name:'Launch',date:mission.launchDate},...mission.majorEncounters,...(mission.missionEndDate?[{name:'Mission end',date:mission.missionEndDate}]:[])];return <section className="mission-timeline"><div className="section-label">MISSION TIMELINE <span>1957 — 2030</span></div><div className="timeline-track"><i style={{left:`${Math.max(0,Math.min(100,(new Date(time).getUTCFullYear()-1957)/73*100))}%`}}/></div><div className="milestones">{events.map((e,i)=><button key={i} onClick={()=>onDate(Date.parse(e.date)+12*3600000)}><small>{dateLabel(Date.parse(e.date))}</small>{e.name}</button>)}<button onClick={()=>onDate(Date.now())}><small>Simulation date</small>Current position</button></div></section>}
