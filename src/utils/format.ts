import {AU,LY,PC,C} from '../data/constants';
export function distance(km:number){const n=Math.abs(km);if(!Number.isFinite(n))return 'Unavailable';if(n<1e6)return `${n.toLocaleString('en',{maximumFractionDigits:0})} km`;if(n<AU*.1)return `${(n/1e6).toFixed(2)} million km`;if(n<LY*.1)return `${(n/AU).toLocaleString('en',{maximumFractionDigits:n/AU>100?0:2})} AU`;if(n<LY*1000)return `${(n/LY).toLocaleString('en',{maximumFractionDigits:2})} ly`;if(n<PC*1e6)return `${(n/PC/1000).toLocaleString('en',{maximumFractionDigits:2})} kpc`;return `${(n/PC/1e6).toFixed(2)} Mpc`;}
export function duration(sec:number){if(!Number.isFinite(sec))return 'Unavailable';if(sec<60)return `${sec.toFixed(1)} seconds`;if(sec<3600)return `${Math.floor(sec/60)} min ${Math.floor(sec%60)} sec`;if(sec<86400*3)return `${Math.floor(sec/3600)} h ${Math.floor(sec%3600/60)} min`;if(sec<31557600)return `${(sec/86400).toFixed(1)} days`;return `${(sec/31557600).toLocaleString('en',{maximumFractionDigits:2})} years`;}
export const lightTime=(km:number)=>duration(km/C);
export const dateLabel=(time:number)=>new Date(time).toLocaleDateString('en-GB',{day:'2-digit',month:'short',year:'numeric',timeZone:'UTC'});
